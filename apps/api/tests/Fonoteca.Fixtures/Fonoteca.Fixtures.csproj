<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Real audio files, one per container and codec, generated by ffmpeg rather
    than committed as binaries — see Corpus.cs.

    Tag writing cannot be tested honestly against mocks: the failure modes that
    matter are ID3v2.3-vs-v2.4 frame handling, multi-value Vorbis comments,
    MP4 atom rewriting and embedded artwork survival. Those only appear when
    real bytes go through a real encoder.
  -->

  <ItemGroup>
    <ProjectReference Include="../../src/Fonoteca.Domain/Fonoteca.Domain.csproj" />
  </ItemGroup>

</Project>

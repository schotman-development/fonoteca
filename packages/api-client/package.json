{
  "name": "@fonoteca/api-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TypeScript client for the Fonoteca API. Types generated from openapi.json; the fetch wrapper is hand-written so the package has no runtime dependencies.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "generate:check": "node scripts/check-drift.ts"
  },
  "devDependencies": {
    "@fonoteca/tsconfig": "workspace:*",
    "@types/node": "^24.10.1",
    "typescript": "7.0.2"
  }
}
